{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "installCommand": "pnpm install",
          "buildCommand": "pnpm run build"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/public/**",
        "headers": { "cache-control": "s-maxage=0" }
      }
    ]
  }
  